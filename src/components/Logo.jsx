import logo from '../images/logo-foot.png';

function Logo() {
    return (
        <div className="logo">
            <img src={logo} alt="Logo" className="logo" />
        </div>
    );
}

export default Logo;